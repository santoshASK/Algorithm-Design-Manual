def insertionsort(arr):
    for i in range(len(arr)):
        j = i
        while(j >0 and (arr[j] < arr[j-1])):
            arr[j],arr[j-1] = arr[j-1],arr[j]
            j -= 1
    
    return

arr = [7,9,5,6,3,8,4,1,2]
insertionsort(arr)
print(arr)
